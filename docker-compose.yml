services:
  chatchat:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - DATA_DIR=/app/data
    volumes:
      - chatchat-data:/app/data

volumes:
  chatchat-data:
